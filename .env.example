# Configuración de Nopolo
# Copia este archivo a .env y configura tus valores

# === Modo de ejecución ===
# dev   → usa las carpetas del proyecto directamente (para desarrollo)
# build → usa AppData/Library del sistema (para el ejecutable distribuido)
# Si no se define, se detecta automáticamente (script=dev, exe=build)
# NOPOLO_ENV=dev

# === Python para subprocesos (solo modo BUILD) ===
# En modo BUILD, Nopolo necesita un intérprete Python real para lanzar
# el worker de RVC. Normalmente lo detecta automáticamente buscando el
# venv con el que fue compilado.
# Si la detección automática falla (ej: moviste la carpeta dist a otro lugar),
# especifica aquí la ruta absoluta al python3 que tiene todas las dependencias:
# NOPOLO_PYTHON=/ruta/al/venv/bin/python3

# === API Configuration ===
API_HOST=0.0.0.0
API_PORT=8000

# === GUI Configuration ===
GUI_AUTO_START_API=false

# === Google Cloud TTS (Opcional) ===
# Si quieres usar Google Cloud TTS, descarga las credenciales JSON
# y coloca la ruta aquí:
# GOOGLE_APPLICATION_CREDENTIALS=config/google-credentials.json

# === RVC Configuration ===
# Device para procesamiento (auto, cuda, cpu, mps)
RVC_DEVICE=auto

# F0 extraction method (rmvpe, parselmouth, harvest)
RVC_F0_METHOD=rmvpe

# === Audio Configuration ===
# Dispositivo de salida de audio (deja vacío para usar por defecto)
AUDIO_OUTPUT_DEVICE=

# === Paths (avanzado - normalmente no hace falta cambiar) ===
# Solo si necesitas override manual de rutas específicas:
# EDGE_VOICES_JSON=config/edge_voices.json

# === Auto-Update / Release (SOLO PARA DESARROLLADORES) ===
# Estas variables NO son necesarias para usuarios finales.
# Solo se usan al ejecutar build_executable.py para subir releases a R2.
# R2_ENDPOINT=https://XXXXXXXXXX.r2.cloudflarestorage.com
# R2_BUCKET=nopolo-updates
# R2_ACCESS_KEY_ID=tu_access_key
# R2_SECRET_ACCESS_KEY=tu_secret_key
